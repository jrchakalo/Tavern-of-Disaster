# Backend Environment Example
# Copy this file to .env and fill the values.

# MongoDB connection string (include credentials / cluster / db name)
MONGO_URI=mongodb://localhost:27017/tavern_of_disaster

# JWT secret key used for signing auth tokens
JWT_SECRET=change_this_secret_in_production

# Port for Express/Socket.IO server (default 3001 if unset)
PORT=3001

# CORS allowed origin(s) (comma separated). Use * only in dev.
CORS_ORIGINS=http://localhost:5173

# Optional: log level (e.g., debug|info|warn|error)
LOG_LEVEL=info

# Optional: token expiration in seconds (e.g., 86400 = 1 day)
JWT_EXPIRES_IN=86400

# ---------------------------------------------------------------------------
# Frontend (Vite) variables would live in frontend/.env.[mode]
# They must be prefixed with VITE_ to be exposed to the client.
# Example (create frontend/.env.local if needed):
# VITE_API_URL=http://localhost:3001
# VITE_SOCKET_URL=http://localhost:3001
# ---------------------------------------------------------------------------
